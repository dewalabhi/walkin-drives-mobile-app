# Force IPv4 connection to avoid IPv6 issues
MONGODB_URI=mongodb://127.0.0.1:27017/walkin-drives

# JWT Secret
JWT_SECRET=walkin_drive_app_super_secret_jwt_key_2025_change_in_production

# Server Configuration
PORT=5000
NODE_ENV=development

# Note: Using 127.0.0.1 instead of localhost to force IPv4 connection
# This fixes the ECONNREFUSED ::1:27017 error on Windows