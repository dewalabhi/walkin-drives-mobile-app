# Try different MongoDB connection formats
# Default local connection
MONGODB_URI=mongodb://127.0.0.1:27017/walkin-drives

# Alternative if above doesn't work
# MONGODB_URI=mongodb://localhost:27017/walkin-drives

# JWT Secret
JWT_SECRET=walkin_drive_app_super_secret_jwt_key_2025_change_in_production

# Server Configuration
PORT=5000
NODE_ENV=development

# Troubleshooting:
# 1. Make sure MongoDB service is running: net start MongoDB
# 2. Test connection with: mongosh
# 3. Check Windows Services for "MongoDB" service
# 4. Alternative: Use Docker - docker run -d -p 27017:27017 mongo:latest
