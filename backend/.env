# Local MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/walkin-drives

# JWT Secret (change this to a random string)
JWT_SECRET=walkin_drive_app_super_secret_jwt_key_2025_change_in_production

# Server Configuration
PORT=5000
NODE_ENV=development

# Instructions:
# 1. Install MongoDB locally (see docs/MONGODB_SETUP.md)
# 2. Make sure MongoDB service is running
# 3. Database "walkin-drives" will be created automatically
# 4. Run: npm run dev to start the backend server